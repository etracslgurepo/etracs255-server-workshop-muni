<schema>
	<element tablename="memoranda_template">
		<field name="objid" primary="true" prefix="MT"/>
		
		<field name="code" caption="Code" required="true" 
			searchable="true"  indexed="true"
			width="60" />

		<field name="template" caption="Template" required="true" 
			searchable="true"  indexed="true"
			width="250"/>

		<complex name="fields" serializer="default"/>
	</element>
</schema>
